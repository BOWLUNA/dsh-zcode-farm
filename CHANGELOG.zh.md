# Changelog

[English](CHANGELOG.md) | **中文**

本文件记录所有值得注意的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.1] — 2026-09-21

### 修复

- **Node 24 上测试闸门被静默关掉了。** `test/run.mjs` 靠匹配输出里的 `# pass N` 来统计套件结果。
  Node 24 把默认测试 reporter 换成了 `spec`（**即使 stdout 不是 TTY**），那一行于是变成 `ℹ pass N`，
  正则一个字都匹配不到，每个套件都解析成「0 通过, 0 失败」。危害不是构建变红：在 Node 24 上，
  跑测试的那一步**一边断言失败一边打印绿勾并 exit 0**，因为它一条失败都没统计到。
  `test` 工作流在那条腿上确实红了 —— 但红在后一步「文档数字」守卫上，它也读同一份统计，
  于是报出五份文档数字不对。报错把人指向文档，照它说的做就是把「0 项检查」写进五份文件，
  这道闸门从此永久失效。

  runner 现在钉死 `--test-reporter=tap`，并且把「读不懂输出」本身当作失败：
  摘要解析不出来的套件计为失败，一条检查都没收集到也算错，而不是算通过。

  这里的症状与根因离得太远，所以修复做了双向验证：注入一条必然失败的断言后，
  runner 在 Node 20 / 22 / 24 上都变红；撤掉那条断言后又恢复绿。
  钉 reporter 这一步也单独用「撤回」验过 —— 撤回后 runner 报「结果无法解析」并 exit 1，
  而不是悄悄放行。

### 备注

- 发布出去的**代码**没有变化。`test/` 不在 `files` 清单里，所以这次修复本身不往包里加任何东西；
  与 `1.0.0` 的差别只有版本号字段和文档。用 `npm pack --dry-run` 核对过：36 个文件，其中没有 `test/`。
- 发布工作流现在会创建 GitHub Release。它的 `permissions` 写的是 `contents: read`，也没有任何一步
  去建 Release，于是给这次发布打上 tag 之后，仓库的 Releases 面板仍停在 `v1.0.0` ——
  代码更新了，首页却像什么都没发生。现在改成 `contents: write`，并加了一步：
  Release 正文直接取本文件里该 tag 对应的小节。

## [1.0.0] — 2026-09-21

首个公开发布版。它之前的一切（0.1.x）都是未发布的开发期快照。

### 包含

- 多实例 ComfyUI 编排：读出每个端点的 GPU、空闲显存与队列深度，再把每个生成任务派给最空闲且满足条件的实例
- 三个工具：`comfyui_farm_status`、`comfyui_farm_pick` 与 `comfyui_farm_run`
- 两段式选择：先按可达性、显存门槛与队列上限过滤，再按「空闲显存 － 队列深 × 权重」排序
- 探测失败自动重试，因为跨 SSH 隧道的一次超时不足以证明实例已下线
- 零运行时依赖 —— 只用 Node 内置的 `fetch` 与 `AbortController`

### 兼容性

- 在 dsh `0.1.5-rc.2` 与 `0.1.6-alpha.2` 上开发并验证过
- 用到的宿主 API：`ctx.tools.register`、`ctx.get`、`ctx.effect`、`ctx.logger`

## [0.1.0] — 2026-09-21

首个版本。

### 新增

- `comfyui_farm_status` —— 农场全景：每台的 GPU、空闲显存、队列深度、在跑什么，以及当前建议派发目标
- `comfyui_farm_pick` —— 只选择不执行：按显存门槛 / 队列上限 / GPU 型号关键字 / 排除名单选实例，并给出理由
- `comfyui_farm_run` —— 负载感知派发：自动选实例或强制指定，支持原始 API 工作流与「提示词 + 模型」一键文生图
- 两段式选实例：先按硬条件过滤，再按 `空闲显存 − 队列深 × queueWeight` 排序
- 探测失败自动重试（跨 SSH 隧道抖动防护），并把 `attempts` 一并报出
- 三个探针：`probe-farm.mjs`（独立状态探测）、`probe-tools.mjs`（加载入口并真实执行工具）

### 实测验证（本机，5 条 SSH 隧道 + 1 个可切换转发口）

- 同一时刻 5 台 RTX 5090 都在跑视频工作流（空闲显存 0.5–0.9 GB），一台 RTX PRO 6000
  Blackwell 完全空闲（99.4 GB），插件**正确识别并选中后者**
- 端到端出图成功：自动派发 → `prompt_id` 返回 → `status: success / completed: true`
  → 产出 `farm_00001_.png`，可经 `/view` 取回（HTTP 200 / 424 466 字节 / image/png）
- 单元测试 15 项全通过（`node --test`）

### 已修复（开发期内）

- **重试缺失**：单次探测失败曾被直接判为「不可达」。实测同一台机器一次 `>3s` 超时、
  紧接着 `106ms` 就返回，说明跨隧道抖动会被误判。现已默认重试 1 次。
- **理由被覆盖**：按 GPU 型号筛选时，不可达的实例原因会被改写成「GPU 不匹配」，
  掩盖真正的问题。现在只对本来就合格的实例做型号筛选。
- **标签失实**：输出里曾把 `/prompt` 返回的 `number` 标成「队列位次」，实测它在队列为空时
  仍返回 22 —— 那是服务端累计序号。标签已更正为「服务端任务号（累计计数，非队列位置）」。
